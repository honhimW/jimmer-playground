plugins {
    id 'io.github.honhimw.misc'
}

def libraries = subprojects.findAll {
    it.buildFile.exists()
}

configure(libraries) {
    group = 'io.github.honhimw'
    pluginManager.apply 'io.github.honhimw.library'
}
